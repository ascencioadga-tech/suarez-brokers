[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Server-side: redirect bare "/" to default locale.
[[redirects]]
  from = "/"
  to = "/en/"
  status = 301
  force = false
